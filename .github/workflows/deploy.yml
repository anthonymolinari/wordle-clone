name: deploy 

on:
  workflow_run:
    workflows: ["build"]
    types:
      - "completed"

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: 'depoy to docker host via ssh'
        run: |
          echo "deploying to local infra"
          docker run -d --name word-guess -p 3210:3000 anthonymolinari/word-guess:latest
